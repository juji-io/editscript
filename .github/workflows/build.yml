name: build

on:
  push:
    paths-ignore:
      - '*.md'
  pull_request:
    paths-ignore:
      - '*.md'

jobs:

   clj-test:
     runs-on: ubuntu-latest
     steps:
       - name: Git checkout
         uses: actions/checkout@v7

       - name: Prepare java
         uses: actions/setup-java@v5
         with:
          distribution: 'temurin'
          java-version: '8'

       - name: Install clojure tools
         uses: DeLaGuardo/setup-clojure@13.6.1
         with:
          # 2.13.0's launcher incorrectly requests 2.12.1-SNAPSHOT.
          lein: 2.12.0

       - name: Run JVM tests
         run: lein test

   bb-test:
     runs-on: ubuntu-latest
     steps:
       - name: Git checkout
         uses: actions/checkout@v7

       - name: Prepare java
         uses: actions/setup-java@v5
         with:
          distribution: 'temurin'
          java-version: '8'

       - name: Install clojure tools
         uses: DeLaGuardo/setup-clojure@13.6.1
         with:
          bb: latest

       - name: Run babashka tests
         run: |
           bb test:bb


   cljr-test:
     runs-on: ubuntu-latest
     timeout-minutes: 20
     steps:
       - name: Git checkout
         uses: actions/checkout@v7

       - name: Prepare .NET
         uses: actions/setup-dotnet@v6
         with:
          dotnet-version: '8.0.x'

       - name: Install ClojureCLR tools
         run: |
           dotnet tool install --global Clojure.Main --version 1.12.2
           dotnet tool install --global Clojure.Cljr --version 0.1.0-alpha11
           echo "$HOME/.dotnet/tools" >> "$GITHUB_PATH"

       - name: Run ClojureCLR tests
         run: cljr -X:test


   cljs-test:
     runs-on: ubuntu-latest
     steps:
       - name: Git checkout
         uses: actions/checkout@v7

       - name: Prepare java
         uses: actions/setup-java@v5
         with:
          distribution: 'temurin'
          java-version: '21'

       - name: Install clojure tools
         uses: DeLaGuardo/setup-clojure@13.6.1
         with:
          # 2.13.0's launcher incorrectly requests 2.12.1-SNAPSHOT.
          lein: 2.12.0

       - uses: actions/setup-node@v7
         with:
          node-version: '24'

       - name: Run node tests
         run: lein doo node once
